# infra/supabase

Pointer. The actual Supabase CLI project lives at [`../../supabase/`](../../supabase/)
because the CLI hard-codes `./supabase/migrations/` as the migrations path.

See [`../../supabase/README.md`](../../supabase/README.md) for setup, the
day-to-day CLI workflow, and the SQL Editor bootstrap fallback.
