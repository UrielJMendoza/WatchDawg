/**
 * Dashboard root. Fleshed out in Task 7 with the three-pane shell
 * (topbar / left-nav / map / detail-panel / timeline). For now this is
 * a minimal placeholder so the dashboard route layout can mount.
 */
export default function DashboardPage() {
  return (
    <main className="flex flex-1 items-center justify-center px-6">
      <p className="font-mono text-xs uppercase tracking-widest text-muted">
        Dashboard shell — awaiting Task 7.
      </p>
    </main>
  );
}
